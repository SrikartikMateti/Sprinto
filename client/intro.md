1) We start with user authentication, follow the docs step by step then we went to, main.jsx added the clerk auth i.e if user is not signed in then we must make him/her sign, update the navbar with the image
2) Creating the backend
server using express, inside the server folder