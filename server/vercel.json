{
	"version": 2,
	"builds": [
		{
			"src": "server.js",
			"use": "@vercel/node",
			"config": {
				"includeFiles": ["dist/**", "configs/**","inngest/**","prisma/**"]
			}
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "server.js"
		}
	]
}
