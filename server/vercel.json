{
	"version": 2,
	"builds": [
		{
			"src": "server.js",
			"use": "@vercel/node",
			"config": {
				"includeFiles": ["dist/**", "server/configs/**","server/inngest/**","server/prisma/**"]
			}
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "server.js"
		}
	]
}
